// get the product from the database to render in Product
import React,  {useState} from 'react';

const productData = props => {
    const [product, setProduct] = useState(null);

    switch(props.type) {
        
    };

    return (
        <p>productData</p>
    );
};

export default productData;